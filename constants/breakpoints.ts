const MIN_XS = 350;
const MIN_SM = 576;
const MIN_MD = 768;
const MIN_LG = 992;
const MIN_XL = 1200;

const MIN_XS_PX = `${MIN_XS}px`;
const MIN_SM_PX = `${MIN_SM}px`;
const MIN_MD_PX = `${MIN_MD}px`;
const MIN_LG_PX = `${MIN_LG}px`;
const MIN_XL_PX = `${MIN_XL}px`;

export const ALL_MIN = {
  xs: MIN_XS_PX,
  sm: MIN_SM_PX,
  md: MIN_MD_PX,
  lg: MIN_LG_PX,
  xl: MIN_XL_PX,
};
